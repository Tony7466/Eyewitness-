# keep embedded version of .js files
embedded-javascript-library please use libjs-jquery [usr/share/eyewitness/bin/jquery*]
# the geckodriver binary is downloaded during build. We can't patch /
# change it
spelling-error-in-binary * [usr/bin/geckodriver]
# the geckodriver binary is statically built
statically-linked-binary [usr/bin/geckodriver]
